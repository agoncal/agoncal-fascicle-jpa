create table Address (
  id bigint not null,
  street1 varchar(255),
  street2 varchar(255),
  city varchar(255),
  state varchar(255),
  zipcode varchar(255),
  country varchar(255),
  primary key (id)
);
